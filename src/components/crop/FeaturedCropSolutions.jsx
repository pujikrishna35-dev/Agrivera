import React from 'react';
import SmartCropSolutionsGrid from './SmartCropSolutionsGrid';

export const FeaturedCropSolutions = (props) => {
  return <SmartCropSolutionsGrid {...props} />;
};

export default FeaturedCropSolutions;
